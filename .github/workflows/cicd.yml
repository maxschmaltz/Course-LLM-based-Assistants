name: cicd

on:
  push:
    branches:
      - main

jobs:

  build-book:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Build the Jupyter Book
        run: |
          jupyter-book build llm-based-assistants/ --html

      - name: Check if gh-pages branch exists
        run: |
          git fetch origin gh-pages || git checkout --orphan gh-pages
        shell: bash

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./llm-based-assistants/_build/html

    # steps:
    #   - name: Checkout repo
    #     uses: actions/checkout@v4

    #   - name: Set up Python
    #     uses: actions/setup-python@v4
    #     with:
    #       python-version: "3.12"
    #       cache: 'pip'

    #   - name: Install dependencies
    #     run: |
    #       python -m pip install --upgrade pip
    #       pip install -r requirements.txt

    #   - name: Build the Jupyter Book
    #     run: |
    #       set -e
    #       jupyter-book build llm-based-assistants
    #       ls -la llm-based-assistants/_build/html

    #   - name: Verify build output exists
    #     run: |
    #       test -f llm-based-assistants/_build/html/index.html || (echo "ERROR: index.html not found under llm-based-assistants/_build/html" && ls -R llm-based-assistants/_build || true && exit 1)

    #   - name: Upload site artifact (for debugging)
    #     uses: actions/upload-artifact@v4
    #     with:
    #       name: site-html
    #       path: llm-based-assistants/_build/html

    #   - name: Deploy to GitHub Pages
    #     uses: peaceiris/actions-gh-pages@v3
    #     with:
    #       github_token: ${{ secrets.ACCESS_TOKEN_FOR_BOT }}
    #       publish_branch: gh-pages
    #       publish_dir: ./llm-based-assistants/_build/html